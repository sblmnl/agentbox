# agentbox project configuration — see agentbox.toml(5)
version = 1

[agents]
install = ["claude-code"]
channel = "stable"

[toolchains]
dotnet = "10"
node   = "24"
python = "3.13"

[network]
mode    = "proxy"
bundles = ["agent:claude-code", "github"]
allow   = []

[security]
min_isolation = "vm"
